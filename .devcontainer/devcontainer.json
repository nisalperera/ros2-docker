{
    "name": "ROS 2 desktop-full",
    "dockerFile": "Dockerfile",
    "runArgs": [
      "--privileged",
      "--network=host",
      "--ipc=host",
      "-e QT_X11_NO_MITSHM=1",
      "--volume=/tmp/.X11-unix:/tmp/.X11-unix"
    ],
    "workspaceFolder": "/home/ubuntu/projects",
    "workspaceMount": "source=${localWorkspaceFolder}/ws/src,target=/home/ubuntu/projects,type=bind",
    "mounts": [
      "source=${localEnv:HOME}${localEnv:USERPROFILE}/.bash_history,target=/home/ubuntu/.bash_history,type=bind",
      "source=${localWorkspaceFolder}/remove.sh,target=/home/ubuntu/projects/remove.sh,type=bind",
      "source=${localWorkspaceFolder}/gazebo_models,target=/usr/share/gazebo-11/models,type=bind",
      "source=${localWorkspaceFolder}/gazebo_worlds,target=/usr/share/gazebo-11/worlds,type=bind"
      // "source=${localWorkspaceFolder}/bashrc,target=/home/ubuntu/.bashrc,type=bind"
    ],
    "postCreateCommand": "chmod +x remove.sh && sh remove.sh",
    
    "build": { 
      "args": {
          "ROS_DISTRO": "humble"
        }
    },
    
    "containerEnv": {
        "DISPLAY": "${localEnv:DISPLAY}"
    },
    
    "remoteEnv": {
        "DOCKER_BUILDKIT": "0"
    }
  }
  